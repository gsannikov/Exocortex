# {emoji} {Display Name}

{One-line description of what this skill does.}

## Commands

| Command | Action |
|---------|--------|
| `{command1}` | {Description of command 1} |
| `{command2}` | {Description of command 2} |
| `{command3}` | {Description of command 3} |

## Workflow

### Step 1: {First Step Name}
{Brief description of what happens in this step.}
→ Load `modules/{module1}.md` for details

### Step 2: {Second Step Name}
{Brief description of what happens in this step.}

### Step 3: {Third Step Name}
{Brief description of what happens in this step.}
→ Load `modules/{module2}.md` for details

## Module Loading

| Trigger | Load |
|---------|------|
| {trigger condition 1} | `modules/{module1}.md` |
| {trigger condition 2} | `modules/{module2}.md` |
| Detailed workflow | `references/workflow.md` |

## Storage

**Location**: `~/exocortex-data/{skill-name}/`

```
{skill-name}/
├── index.yaml          # Main data index
└── {subfolder}/        # Additional data
```

## Integration

- **Input**: {Where data comes from}
- **Output**: {What this skill produces}

---
**Version**: 0.1.0 | **Patterns**: {pattern1}, {pattern2}
